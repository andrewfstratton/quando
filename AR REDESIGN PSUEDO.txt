When Marker
    
    if theres NOT a scene
        make scene
        make hiddenCanvas
        make Marker
        make camera
        add event listener for scan which will
            do all the shit in box (IF it's not already there??)
    if there IS a scene
        if the marker does NOT exist
            make Marker
            add event listener for scan which will
                do all the shit in box (IF it's not already there??)
            append everything to DOM
        if the marker DOES exist
            alert


--- SHow thing

    if there's NOT a scene
        alert
    if there IS a scene
        get the markerID from the ancestor block?
        if the thing's NOT already there (use filename+markerid as ID?)
            apply orientation rotations n that
            place thing on that
        else 
            alert